module.exports = {
  theme: {
    extend: {
      height: {
        80: '20rem',
      },
      transitionTimingFunction: {
        ease: 'ease',
      },
      gridTemplateColumns: {
        'fill-120': 'repeat(auto-fill, minmax(30rem, 1fr))',
      },
      textColor: {
        current: 'currentColor',
      },
    },
  },
  variants: {},
  plugins: [],
};
